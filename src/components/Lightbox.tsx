export function Lightbox() {
  return (
    <div className="rounded-[1.5rem] border border-dashed border-ink/20 bg-white/30 px-5 py-4 text-sm leading-7 text-ink/55">
      Lightbox remains optional for a later phase so the first version stays calm and focused.
    </div>
  );
}
